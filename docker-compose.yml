services:
  iperf-web-client:
    build: .
    container_name: iperf-web-client
    network_mode: host
    cap_add:
      - NET_ADMIN
    restart: unless-stopped
    environment:
      - SECRET_KEY=change-me-in-production
